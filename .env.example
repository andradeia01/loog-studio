# =============================================================================
# LOOG Studio — variáveis de ambiente
# =============================================================================
# Copie este arquivo para .env.local (desenvolvimento) OU cadastre em
# Site settings → Environment variables (Netlify, em produção).
# =============================================================================

# --- Supabase (OBRIGATÓRIO para auth + aprovação + artes prontas) -----------
# Encontrado em: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi....
# NUNCA exponha no cliente. Apenas server-side.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi....
